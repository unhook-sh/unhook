diff --git a/dist/changesets-cli.cjs.js b/dist/changesets-cli.cjs.js
index 82ed39a6b2a38fe0aaabb71c2ec745c502dbe06a..0fb2528b1659c29d5d05e5fb27c1e13e83d72bfa 100644
--- a/dist/changesets-cli.cjs.js
+++ b/dist/changesets-cli.cjs.js
@@ -634,6 +634,10 @@ async function getPublishTool(cwd) {
   const pm = await packageManagerDetector.detect({
     cwd
   });
+  // Add Bun support
+  if (pm && pm.name === "bun") {
+    return { name: "bun" };
+  }
   if (!pm || pm.name !== "pnpm") return {
     name: "npm"
   };
@@ -764,16 +768,27 @@ async function internalPublish(packageJson, opts, twoFactorState) {
   const envOverride = {
     [scope ? `npm_config_${scope}:registry` : "npm_config_registry"]: registry
   };
-  let {
-    code,
-    stdout,
-    stderr
-  } = publishTool.name === "pnpm" ? await spawn__default["default"]("pnpm", ["publish", "--json", ...publishFlags], {
-    env: Object.assign({}, process.env, envOverride),
-    cwd: opts.cwd
-  }) : await spawn__default["default"](publishTool.name, ["publish", opts.publishDir, "--json", ...publishFlags], {
-    env: Object.assign({}, process.env, envOverride)
-  });
+  let result;
+  if (publishTool.name === "pnpm") {
+    result = await spawn__default["default"]("pnpm", ["publish", "--json", ...publishFlags], {
+      env: Object.assign({}, process.env, envOverride),
+      cwd: opts.cwd
+    });
+  } else if (publishTool.name === "bun") {
+    // Bun: use `bun publish` with supported flags
+    // Note: Bun does not support --json or --otp for publish
+    // See: https://bun.sh/docs/cli/publish
+    const bunFlags = ["publish", opts.publishDir, ...publishFlags];
+    result = await spawn__default["default"]("bun", bunFlags, {
+      env: Object.assign({}, process.env, envOverride),
+      cwd: opts.cwd
+    });
+  } else {
+    result = await spawn__default["default"](publishTool.name, ["publish", opts.publishDir, "--json", ...publishFlags], {
+      env: Object.assign({}, process.env, envOverride)
+    });
+  }
+  let { code, stdout, stderr } = result;
   if (code !== 0) {
     // NPM's --json output is included alongside the `prepublish` and `postpublish` output in terminal
     // We want to handle this as best we can but it has some struggles:
diff --git a/dist/changesets-cli.esm.js b/dist/changesets-cli.esm.js
index 1e945455b39b9c6424b26960b3d26a095ad980e4..9e1067a45178ed35d9ec758316d2bce866fa3a26 100644
--- a/dist/changesets-cli.esm.js
+++ b/dist/changesets-cli.esm.js
@@ -596,6 +596,10 @@ async function getPublishTool(cwd) {
   const pm = await detect({
     cwd
   });
+  // Add Bun support
+  if (pm && pm.name === "bun") {
+    return { name: "bun" };
+  }
   if (!pm || pm.name !== "pnpm") return {
     name: "npm"
   };
@@ -726,16 +730,27 @@ async function internalPublish(packageJson, opts, twoFactorState) {
   const envOverride = {
     [scope ? `npm_config_${scope}:registry` : "npm_config_registry"]: registry
   };
-  let {
-    code,
-    stdout,
-    stderr
-  } = publishTool.name === "pnpm" ? await spawn$1("pnpm", ["publish", "--json", ...publishFlags], {
-    env: Object.assign({}, process.env, envOverride),
-    cwd: opts.cwd
-  }) : await spawn$1(publishTool.name, ["publish", opts.publishDir, "--json", ...publishFlags], {
-    env: Object.assign({}, process.env, envOverride)
-  });
+  let result;
+  if (publishTool.name === "pnpm") {
+    result = await spawn$1("pnpm", ["publish", "--json", ...publishFlags], {
+      env: Object.assign({}, process.env, envOverride),
+      cwd: opts.cwd
+    });
+  } else if (publishTool.name === "bun") {
+    // Bun: use `bun publish` with supported flags
+    // Note: Bun does not support --json or --otp for publish
+    // See: https://bun.sh/docs/cli/publish
+    const bunFlags = ["publish", opts.publishDir, ...publishFlags];
+    result = await spawn$1("bun", bunFlags, {
+      env: Object.assign({}, process.env, envOverride),
+      cwd: opts.cwd
+    });
+  } else {
+    result = await spawn$1(publishTool.name, ["publish", opts.publishDir, "--json", ...publishFlags], {
+      env: Object.assign({}, process.env, envOverride)
+    });
+  }
+  let { code, stdout, stderr } = result;
   if (code !== 0) {
     // NPM's --json output is included alongside the `prepublish` and `postpublish` output in terminal
     // We want to handle this as best we can but it has some struggles:
diff --git a/package.json b/package.json
index 5692e2309778713fd5ee3efb38ce1ffa4042a57d..0353db8c27a5016a1b67f7be006290d289241ad9 100644
--- a/package.json
+++ b/package.json
@@ -88,7 +88,7 @@
     "fs-extra": "^7.0.1",
     "mri": "^1.2.0",
     "p-limit": "^2.2.0",
-    "package-manager-detector": "^0.2.0",
+    "package-manager-detector": "^1.3.0",
     "picocolors": "^1.1.0",
     "resolve-from": "^5.0.0",
     "semver": "^7.5.3",
